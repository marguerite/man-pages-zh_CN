<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.4//EN"
                   "http://www.oasis-open.org/docbook/xml/4.4/docbookx.dtd">
<!-- lifted from man+troff by doclifter -->
<refentry id='chmod1'>
<!-- DO NOT MODIFY THIS FILE!  It was generated by help2man 1.43.3. -->
<refmeta>
<refentrytitle>CHMOD</refentrytitle>
<manvolnum>1</manvolnum>
<refmiscinfo class='source'>July 2014</refmiscinfo>
<refmiscinfo class='manual'>GNU coreutils 8.22</refmiscinfo>
</refmeta>
<refnamediv>
<refname>chmod</refname>
<refpurpose>change file mode bits</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv id='synopsis'>
<cmdsynopsis>
  <command>chmod</command>    
    <arg choice='opt' rep='repeat'><replaceable>OPTION</replaceable></arg>
    <arg choice='plain'><replaceable>MODE</replaceable></arg>
    <arg choice='opt' rep='repeat'><replaceable>,MODE</replaceable></arg>
    <arg choice='plain' rep='repeat'><replaceable>FILE</replaceable></arg>
    
</cmdsynopsis>
<cmdsynopsis>
  <command>chmod</command>    
    <arg choice='opt' rep='repeat'><replaceable>OPTION</replaceable></arg>
    <arg choice='plain'><replaceable>OCTAL-MODE</replaceable></arg>
    <arg choice='plain' rep='repeat'><replaceable>FILE</replaceable></arg>
    
</cmdsynopsis>
<cmdsynopsis>
  <command>chmod</command>    
    <arg choice='opt' rep='repeat'><replaceable>OPTION</replaceable></arg>
    <arg choice='plain'>--reference=<replaceable>RFILE</replaceable></arg>
    <arg choice='plain' rep='repeat'><replaceable>FILE</replaceable></arg>
    
</cmdsynopsis>
</refsynopsisdiv>


<refsect1 id='description'><title>DESCRIPTION</title>
<para>This manual page
documents the GNU version of
<command>chmod</command>.
<command>chmod</command>
changes the file mode bits of each given file according to
<emphasis remap='I'>mode</emphasis>,
which can be either a symbolic representation of changes to make, or
an octal number representing the bit pattern for the new mode bits.</para>

<para>The format of a symbolic mode is [<emphasis remap='B'>ugoa</emphasis>.&thinsp;.&thinsp;.][[<emphasis remap='B'>+-=</emphasis>][<emphasis remap='I'>perms</emphasis>.&thinsp;.&thinsp;.].&thinsp;.&thinsp;.],
where
<emphasis remap='I'>perms</emphasis>
is either zero or more letters from the set
<emphasis remap='B'>rwxXst</emphasis>, or a single letter from the set <emphasis remap='B'>ugo</emphasis>.
Multiple symbolic
modes can be given, separated by commas.</para>

<para>A combination of the letters <emphasis remap='B'>ugoa</emphasis> controls which users' access
to the file will be changed: the user who owns it (<emphasis remap='B'>u</emphasis>), other
users in the file's group (<emphasis remap='B'>g</emphasis>), other users not in the file's
group (<emphasis remap='B'>o</emphasis>), or all users (<emphasis remap='B'>a</emphasis>).  If none of these are given,
the effect is as if <emphasis remap='B'>a</emphasis> were
given, but bits that are set in the umask are not affected.</para>

<para>The operator <emphasis remap='B'>+</emphasis> causes the selected file mode bits to be added to
the existing file mode bits of each file; <emphasis remap='B'>-</emphasis> causes them to be
removed; and <emphasis remap='B'>=</emphasis> causes them to be added and causes unmentioned
bits to be removed except that a directory's unmentioned set user and
group ID bits are not affected.</para>

<para>The letters <emphasis remap='B'>rwxXst</emphasis> select file mode bits for the affected users:
read (<emphasis remap='B'>r</emphasis>), write (<emphasis remap='B'>w</emphasis>), execute (or search for directories)
(<emphasis remap='B'>x</emphasis>), execute/search only if the file is a directory or already
has execute permission for some user (<emphasis remap='B'>X</emphasis>), set user or group ID
on execution (<emphasis remap='B'>s</emphasis>), restricted deletion flag or sticky bit
(<emphasis remap='B'>t</emphasis>).  Instead of one or more of these letters, you can specify
exactly one of the letters <emphasis remap='B'>ugo</emphasis>: the permissions granted to the
user who owns the file (<emphasis remap='B'>u</emphasis>), the permissions granted to other
users who are members of the file's group (<emphasis remap='B'>g</emphasis>),
and the permissions granted to users that are in neither of the two preceding
categories (<emphasis remap='B'>o</emphasis>).</para>

<para>A numeric mode is from one to four octal digits (0-7), derived by
adding up the bits with values 4, 2, and 1.  Omitted digits are
assumed to be leading zeros.
The first digit selects the set user ID (4) and set group ID (2) and
restricted deletion or sticky (1) attributes.  The second digit
selects permissions for the user who owns the file: read (4), write (2),
and execute (1); the third selects permissions for other users in the
file's group, with the same values; and the fourth for other users not
in the file's group, with the same values.</para>

<para><command>chmod</command>
never changes the permissions of symbolic links; the
<command>chmod</command>
system call cannot change their permissions.  This is not a problem
since the permissions of symbolic links are never used.
However, for each symbolic link listed on the command line,
<command>chmod</command>
changes the permissions of the pointed-to file.
In contrast,
<command>chmod</command>
ignores symbolic links encountered during recursive directory
traversals.</para>
</refsect1>

<refsect1 id='setuid_and_setgid_bits'><title>SETUID AND SETGID BITS</title>
<para><command>chmod</command>
clears the set-group-ID bit of a
regular file if the file's group ID does not match the user's
effective group ID or one of the user's supplementary group IDs,
unless the user has appropriate privileges.  Additional restrictions
may cause the set-user-ID and set-group-ID bits of
<emphasis remap='I'>MODE</emphasis>
or
<emphasis remap='I'>RFILE</emphasis>
to be ignored.  This behavior depends on the policy and
functionality of the underlying
<command>chmod</command>
system call.  When in
doubt, check the underlying system behavior.</para>

<para><command>chmod</command>
preserves a directory's set-user-ID and set-group-ID bits unless you
explicitly specify otherwise.  You can set or clear the bits with
symbolic modes like
<emphasis remap='B'>u+s</emphasis>
and
<emphasis remap='B'>g-s</emphasis>,
and you can set (but not clear) the bits with a numeric mode.</para>
</refsect1>

<refsect1 id='restricted_deletion_flag_or_sticky_bit'><title>RESTRICTED DELETION FLAG OR STICKY BIT</title>
<para>The restricted deletion flag or sticky bit is a single bit, whose
interpretation depends on the file type.  For directories, it prevents
unprivileged users from removing or renaming a file in the directory
unless they own the file or the directory; this is called the
<emphasis remap='I'>restricted deletion flag</emphasis>
for the directory, and is commonly found on world-writable directories
like <filename>/tmp</filename>.  For regular files on some older systems, the bit
saves the program's text image on the swap device so it will load more
quickly when run; this is called the
<emphasis remap='I'>sticky bit</emphasis>.</para>
</refsect1>

<refsect1 id='options'><title>OPTIONS</title>
<para>Change the mode of each FILE to MODE.
With <option>--reference</option>, change the mode of each FILE to that of RFILE.</para>
<variablelist remap='TP'>
  <varlistentry>
  <term><option>-c</option>, <option>--changes</option></term>
  <listitem>
<para>like verbose but report only when a change is made</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-f</option>, <option>--silent</option>, <option>--quiet</option></term>
  <listitem>
<para>suppress most error messages</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-v</option>, <option>--verbose</option></term>
  <listitem>
<para>output a diagnostic for every file processed</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>--no-preserve-root</option></term>
  <listitem>
<para>do not treat '/' specially (the default)</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>--preserve-root</option></term>
  <listitem>
<para>fail to operate recursively on '/'</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>--reference</option>=<emphasis remap='I'>RFILE</emphasis></term>
  <listitem>
<para>use RFILE's mode instead of MODE values</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-R</option>, <option>--recursive</option></term>
  <listitem>
<para>change files and directories recursively</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>--help</option></term>
  <listitem>
<para>display this help and exit</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>--version</option></term>
  <listitem>
<para>output version information and exit</para>

<para>Each MODE is of the form '[ugoa]*([-+=]([rwxXst]*|[ugo]))+|[-+=][0-7]+'.</para>

<para>GNU coreutils online help: &lt;<ulink url='http://www.gnu.org/software/coreutils/&gt;'>http://www.gnu.org/software/coreutils/&gt;</ulink>
Report chmod translation bugs to &lt;<ulink url='http://translationproject.org/team/&gt;'>http://translationproject.org/team/&gt;</ulink></para>
  </listitem>
  </varlistentry>
</variablelist>
</refsect1>

<refsect1 id='author'><title>AUTHOR</title>
<para>Written by David MacKenzie and Jim Meyering.</para>
</refsect1>

<refsect1 id='copyright'><title>COPYRIGHT</title>
<para>Copyright &copy; 2013 Free Software Foundation, Inc.
License GPLv3+: GNU GPL version 3 or later &lt;<ulink url='http://gnu.org/licenses/gpl.html&gt;'>http://gnu.org/licenses/gpl.html&gt;</ulink>.
<!-- br -->
This is free software: you are free to change and redistribute it.
There is NO WARRANTY, to the extent permitted by law.</para>
</refsect1>

<refsect1 id='see_also'><title>SEE ALSO</title>
<para><citerefentry><refentrytitle>chmod</refentrytitle><manvolnum>2</manvolnum></citerefentry></para>

<para>The full documentation for
<command>chmod</command>
is maintained as a Texinfo manual.  If the
<emphasis remap='B'>info</emphasis>
and
<command>chmod</command>
programs are properly installed at your site, the command</para>

<para><emphasis remap='B'>info coreutils &apos;'chmod invocation&apos;'</emphasis></para>

<para>should give you access to the complete manual.</para>
</refsect1>
</refentry>

